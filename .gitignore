# Virtual environments
.venv/
venv/

# Python cache
__pycache__/
*.pyc

# Secrets
.env

# Editor settings (personal, not shared)
.vscode/

# Scratch folder for files moved off disk from a mounted device session
# (device_bash can't delete files directly, only move them here — safe to
# empty this folder yourself any time)
_to_delete/
